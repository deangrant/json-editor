/** Document size (bytes) above which the UI warns about large files. */
export const LARGE_DOCUMENT_BYTES = 2 * 1024 * 1024;

/** Document size (bytes) above which tree/table mode is discouraged. */
export const HUGE_DOCUMENT_BYTES = 16 * 1024 * 1024;
